import { 
  regionAll, regionFirstlevel, regionChildren,
} from './region'
import { 
  departmentFirstlevel, departmentId,
} from './department'

export default {
  regionAll, regionFirstlevel, regionChildren,
  departmentFirstlevel, departmentId,
}