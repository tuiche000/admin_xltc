import { 
  regionAll, regionFirstlevel, regionChildren, regionAdd, 
} from './region'
import { 
  departmentFirstlevel, departmentId,
} from './department'

export default {
  regionAll, regionFirstlevel, regionChildren, regionAdd,
  departmentFirstlevel, departmentId,
}